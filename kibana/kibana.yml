server.name: kibana
server.host: "0"
elasticsearch.hosts:
  - "http://1homez-es1:9200"

# elasticsearch.username: elastic
# elasticsearch.password: elastic

xpack.monitoring.ui.container.elasticsearch.enabled: true